# Copy this file to .streamlit/secrets.toml for LOCAL development,
# and put the same key/value in the "Secrets" box on Streamlit Community
# Cloud for the DEPLOYED app. Never commit the real secrets.toml file.

GROQ_API_KEY = "your-groq-api-key-here"
